return {
	"nvim-treesitter/nvim-treesitter",
	opt = {
		ensure_installed = {"lua",},
		highlight = {
			enable = true,
		},
		sync_install = false,
		indent = { enable = true, },
		auto_install = true,
	},
}
